ktor:
  application:
    modules:
      - com.mk.kmpshowcase.server.ApplicationKt.module
  deployment:
    port: "$PORT:8080"
    host: "0.0.0.0"

jwt:
  secret: "$JWT_SECRET:development-only-secret"
  issuer: "$JWT_ISSUER:mkdigital-showcase"
  audience: "$JWT_AUDIENCE:mkdigital-showcase-users"

database:
  useH2: "$USE_H2:true"
  url: "$DATABASE_URL:jdbc:postgresql://localhost:5432/showcase"
  user: "$DATABASE_USER:postgres"
  password: "$DATABASE_PASSWORD:postgres"

cors:
  allowedHosts: "$CORS_ALLOWED_HOSTS:*"
