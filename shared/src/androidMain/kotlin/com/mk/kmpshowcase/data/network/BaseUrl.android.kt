package com.mk.kmpshowcase.data.network

// Production host. Per-build-variant local dev (Android emulator 10.0.2.2) returns with the env-split — see roadmap.
internal actual val BASE_URL = "kmp-showcase-production.up.railway.app"
